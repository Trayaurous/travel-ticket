# Travel Ticket

这是从 Color Studio 票根编辑页拆出的纯前端静态版本，可直接部署到 GitHub Pages。

## 使用方式

- 直接打开 `index.html`。
- 点击右上角图库按钮上传图片。
- 当前没有图片时，也可以点击左侧预览区域上传图片。
- 所有颜色提取、预览和 PNG 导出都在浏览器本地完成，不依赖后端。

## 文件

- `index.html`：页面结构
- `styles.css`：新拟物界面样式
- `app.js`：图片上传、主色提取、票根绘制和导出逻辑
